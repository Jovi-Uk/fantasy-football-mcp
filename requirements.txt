# Fantasy Football MCP Server — Dependencies
#
# Install with: pip install -r requirements.txt
# Or with uv:   uv pip install -r requirements.txt

# MCP SDK — the official Model Context Protocol Python SDK
# This gives us FastMCP, the high-level server framework
mcp>=1.8.0

# Async PostgreSQL driver — needed for pgvector memory
# asyncpg is the fastest async Postgres driver for Python
asyncpg>=0.29.0

# Async HTTP client — for calling the Sleeper API
# httpx is the modern replacement for requests, with async support
httpx>=0.27.0

# Data validation — Pydantic v2 for input schemas
# FastMCP uses these to auto-generate tool schemas
pydantic>=2.0.0

# Numerical operations — used by the embedding fallback
numpy>=1.24.0

# Sentence Transformers — for semantic embeddings (pgvector memory search)
sentence-transformers>=2.2.0

# OPTIONAL: pgvector Python helpers
# Not strictly required (we use raw SQL) but useful for testing
# pgvector>=0.2.0
